# Issue 247

This branch closes the issue in its own commit message; the squash-merge
commit onto main deliberately omits the closing reference, so the action
must recover the link from the pull request rather than the commit text.
