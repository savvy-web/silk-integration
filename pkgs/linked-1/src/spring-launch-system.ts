/**
 * Spring Launch System
 *
 * A safer alternative to the deprecated cannon-launching API.
 * Uses spring-loaded mechanisms instead of dangerous gunpowder
 * for launching circus performers into the stratosphere.
 *
 * BREAKING CHANGE: All launchFromCannon() calls must migrate
 * to springLaunch() for continued aerial entertainment.
 *
 * @packageDocumentation
 */
export {};
